<%# Removed the below line as it causes duplicate comment partials to be rendered %>
<%# turbo_stream.append(@comment, partial: "comments/comment", locals: { comment: @comment }) %>

<%= render_turbo_stream_flash_messages %>