<%# render "shared/form_errors", object: friendship %>
<%= form_with url: user_friendships_path(sender) do |f| %>
  <%= f.hidden_field :friend_id, value: friend.id %>
  <%= f.submit "Accept", class: "btn btn-info" %>
<% end %>