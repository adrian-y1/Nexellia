<%= form_with url: user_friendship_path(friend, destroyer), method: :delete do |f| %>
  <%= f.hidden_field :friend_id, value: friend.id %>
  <%= f.submit "Remove", class: "btn btn-danger" %>
<% end %>