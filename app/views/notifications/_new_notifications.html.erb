<% if current_user %>
  <div class="container">
    <%= turbo_stream_from "new_notifications_#{user.id}" %>
    
    <%= turbo_frame_tag "new_notifications_#{user.id}" do %>
      <% @unread_notifications.each do |notification| %>
        <%= render "notifications/new_notification", notification: notification %>
      <% end %>
    <% end %>
  </div>
<% end %>